import 'package:flutter/material.dart';

class Triarcs extends StatelessWidget {
  const Triarcs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}